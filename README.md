# hello world written in RISC-V assembly (linux)

This is a classic ```hello world``` written in 64 bits RISC-V assembly.  

Makefile will build with debug symbols, so you can have some fun with gdb :-)
